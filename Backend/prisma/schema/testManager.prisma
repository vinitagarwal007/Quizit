model testManager{
    tmid Int @id @default(autoincrement())
    tid Int
    uid Int
    started_at DateTime
    ended_at DateTime
    violation Int @default(0)

    user user @relation(fields: [uid], references: [uid])
    test test @relation(fields: [tid], references: [tid])
}